---
const { tags } = Astro.props
---

<div class="tag-container">
	{
		tags.sort().map((tag: string) => (
			<a class="tag" href={`/archive/${tag}`}>
				{tag}
			</a>
		))
	}
</div>

<style>
	.tag-container {
		margin: 10px 0;
	}

	.tag {
		font-size: 0.8rem;
		color: var(--text);
		padding: 4px 8px;
		border-radius: 25px;
		background: var(--blue);
		line-height: 2rem;
	}

	.tag:not(:last-child) {
		margin-right: 6px;
	}
</style>
