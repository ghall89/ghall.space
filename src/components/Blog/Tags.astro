---
import PillList from '@components/PillList.astro';
import { getSlug } from 'src/utils/tags';

interface Props {
	tags: string[];
}

const { tags } = Astro.props;
---

<PillList
	items={tags}
	hrefBuilder={(tag: string) => `/blog/tag/${encodeURIComponent(getSlug(tag))}`}
/>
